(() => {
  // Dinosaur artwork is now owned by the typed React DinosaurArt component.
  // This compatibility file intentionally performs no DOM mutation so older
  // cached HTML can continue loading it without duplicating cards or timers.
  window.__NICOS_WORLD_REACT_DINOSAUR_ART__ = true;
})();
